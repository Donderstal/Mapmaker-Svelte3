export enum AnimationTypeEnum {
    idle            = "IDLE",
    semiIdle        = "SEMI_IDLE",
    moving          = "MOVING",
    movingLoop      = "MOVING_IN_LOOP",
    animationLoop   = "ANIMATION_LOOP"
}